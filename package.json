{
  "name": "open-abap-bal",
  "version": "1.0.0",
  "private": true,
  "description": "test",
  "scripts": {
    "lint": "abaplint",
    "unit": "rm -rf output && abap_transpile && echo RUNNING && node output/index.mjs",
    "test": "npm run lint && npm run unit"
  },
  "license": "MIT",
  "dependencies": {
    "@abaplint/cli": "^2.119.37",
    "@abaplint/runtime": "^2.13.30",
    "@abaplint/database-sqlite": "^2.11.78",
    "@abaplint/transpiler-cli": "^2.13.30"
  }
}
